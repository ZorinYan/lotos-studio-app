# Скопируйте в переменные окружения Vercel (Settings → Environment Variables)
# URL вашего API (Render, Railway, VPS и т.д.) — без слэша в конце
VITE_API_BASE=https://your-api.example.com

# В продакшене VK Bridge должен быть включён (не задавайте VITE_SKIP_VK_BRIDGE)
